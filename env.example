# BuildAI Arena Environment Variables
# Copy this file to .env.local and fill in your actual values

# Supabase Configuration
NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# Groq AI Configuration
GROQ_API_KEY=your_groq_api_key

# Resend Email Configuration
RESEND_API_KEY=your_resend_api_key

# Base URL for email links and auth callbacks (default: http://localhost:3000)
# In production, set this to your production URL: https://classly.space
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# Site URL for auth redirects (optional, falls back to NEXT_PUBLIC_BASE_URL)
# Set this in production to ensure OAuth callbacks use the correct URL
# NEXT_PUBLIC_SITE_URL=https://classly.space

# GitHub OAuth Configuration (configured in Supabase Dashboard)
# No need to add these here as they're configured in Supabase Auth settings
# GITHUB_CLIENT_ID=your_github_client_id
# GITHUB_CLIENT_SECRET=your_github_client_secret

# Optional: Supabase MCP Access Token (for development tools)
# SUPABASE_ACCESS_TOKEN=your_supabase_access_token
